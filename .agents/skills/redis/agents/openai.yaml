interface:
  display_name: "Redis"
  short_description: "Repo-specific patterns and workflows for redis"
  default_prompt: "Use the redis repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true